import * as Y from "yjs";
import * as moonbit from "../../../../_build/js/release/build/boundary/boundary.js";
export const duplicateComposition = (doc, sceneId, compositionId) => moonbit.duplicateComposition(doc, sceneId, compositionId, Y);
export const deleteComposition = (doc, sceneId, compositionId) => moonbit.deleteComposition(doc, sceneId, compositionId, Y);
