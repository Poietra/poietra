import type { RenderObject } from '../evaluate';
export declare const TEXT_CLIP_VERTICAL_EM = 0.2;
export declare function textMarkup(item: RenderObject, prefix: string, glyphOpacity?: (index: number) => number): string;
