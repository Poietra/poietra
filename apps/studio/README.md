# Poietra Studio

**友人と、AI と。動きを一緒につくる。**

Poietra は、同じ URL を開いたメンバーで動画を共同編集するブラウザアプリです。
図形・文字・数式・画像・動画を配置し、属性ごとの動きと音声を組み合わせて書き出せます。
AI に頼んだ編集も、同じオブジェクトの位置・色・時間として残り、人間が調整できます。

このリポジトリでは UI・編集エンジン・サーバーのアプリケーションロジックを MoonBit で実装しています。
数値カーネルは WASM、ブラウザ・サーバーとの接続は JavaScript ターゲットです。
`src`・`shared`・`server`・`worker` の実行用 TS/TSX は削除しました。
公開 API の型宣言とテスト用 TypeScript は維持し、Node サーバーは TS ローダーなしで起動します。
**2026-09-20 02:55 JST から [poietra.com](https://poietra.com) で MoonBit 版を公開しています。**
既存 Worker を更新し、共有ルーム・素材・アカウントの保存先と設定を引き継いでいます。

GitHub の言語表示は、テストなどを含むソース容量の比率です。2026-09-20 に比較用の旧実装を削除しました。
`0cda0c5` で再集計した残る TS はテスト 16,378 行、型宣言 1,934 行、計測・設定 140 行で、
実行用 TS/TSX は 0 行です。MoonBit のアプリ実装は 56,786 行、ネイティブ JS アダプターは 783 行あります。
生成コード・空行・コメントを含む集計で、外部ライブラリの実装や配信容量は含みません。
React・Yjs・Mediabunny などへの JavaScript 実行時依存は残っており、その置換は別の作業です。
内部の型付きコアへの整理も継続中です。音声・動画の編集、UI・ホストの一部には
`Any` と JS オブジェクト操作を使う処理が残っています。実行用 TS が 0 であることと、この整理の完了は別々に確認します。
ルートの `pnpm audit:source` で内訳を再集計でき、実行用 TS の再混入も CI で検出します。
CPU 計測は Node 24 の標準機能で起動するように変更し、`tsx` ローダーを依存から削除しました。
`pnpm bench` は現行の MoonBit 実装だけを計測します。旧実装が必要な場合は
[poietra-hackathon](https://github.com/Poietra/poietra-hackathon) または Git の履歴を参照できます。

[セットアップ・構成・最新の性能計測](../../README.md) · [開発方針](AGENTS.md) · [不具合の報告](https://github.com/Poietra/poietra/issues)

ブラウザ用ビルドは UI・文書・編集・Undo・描画・素材の6パッケージを `client_runtime` にまとめてリンクし、
共通の MoonBit コードを重複配信しない構成です。パッケージの責務と公開 JS API は維持し、
公開関数の一覧を `moon.pkg` から生成します。新機能の型・APIは単独出力と共通出力の両方で拡張テストを行います。
Node・Worker・SSR は単独出力を使い、トップページ・ファイル操作・書き出し・MathJax・Mediabunny は別に読み込みます。

## 制作を始める

[ルートの手順](../../README.md#run-locally)でツールチェーンを用意し、リポジトリのルートで `pnpm dev` を実行します。
`http://localhost:5173` のトップページから、新しいプロジェクトまたは編集可能なサンプルを開いてください。
トップページの閲覧だけでは部屋を作成せず、編集エンジンも読み込みません。

編集画面の左上にある Poietra ロゴが **New project / Open project / Save project** の入口です。
共有リンクを別のブラウザで開くと同じ部屋に参加できます。リンクを知っている人は編集可能で、ログインは必須ではありません。

## 場面と動きを組み立てる

| 単位 | 役割 |
| --- | --- |
| Scene | キャンバスと、その中のオブジェクト・場面・音声。プロジェクト内の順番で再生する。 |
| Composition | オブジェクトの配置や見た目を持つ静止状態。指定した時間、その状態を保つ。 |
| Transition | 隣り合う Composition 間の変化。オブジェクトや属性ごとに動きを設定する。 |

例えば「円が左にある Composition → 移動する Transition → 円が右にある Composition」で移動を作れます。
オブジェクトの識別は Scene 共通ですが、位置・色・大きさ・表示状態は Composition ごとに独立しています。

Composition の **＋** は最後の状態を複製して次の場面を追加します。
Transition では **Move / Write / Fade / Grow / Cut**、開始時刻・長さ・イージングを設定します。
Scene は追加・複製・名前変更・削除・並べ替えに対応しています。

**Property timing** では、位置を 2,000 ms で動かし、透明度だけ最初の 300 ms で変える、といった設定ができます。
透明度の始点・終点は前後の Composition で指定します。属性別のバーはタイムラインにも表示され、ドラッグで調整できます。
個別設定がない属性は共通 Timing を継承し、**共通の時間に戻す** で個別設定を解除します。

**Easing → Custom Bézier** は速度変化の曲線です。制御点の座標は `0〜1`、オーバーシュートは対象外です。
Move の **Edit Bézier path** は空間上の移動経路です。速度の曲線と移動経路は別に編集します。

**Keyframes → 中間点を追加** で、選んだ属性に途中の実際の値を置けます。
例えば前後の Composition を `x=100` にし、X の Time を `50%`、Value を `300` にすると往復します。
時刻は属性の区間内の割合なので、Duration を伸ばすと中間点も比例して遅くなります。
両端の値は Composition に連動します。各点の Easing は次の点へ向かう区間、最初の区間は属性の Easing を使います。
中間点を削除しても他の人の編集は残り、自分の削除は Undo できます。Cut では中間点を保持したまま無効にします。

## キャンバスを編集する

ツールからオブジェクトを追加し、右側の **Design** で位置・大きさ・外観を調整します。
テキストと数式はダブルクリックで内容を編集できます。
空白をドラッグすると範囲選択、Shift で選択追加、Group / Ungroup で連結します。
連結したメンバーをドラッグすると、表示中でロックされていないメンバーも一緒に動きます。

**Transform → Parent** は Scene 全体で共通の親子関係です。親の移動・回転・拡縮に子が追従します。
親を選ぶ・None で解除する操作は、各 Composition の見た目を保つように子の座標を変換します。
位置・回転・Scale X/Y・Anchor X/Y は Composition ごとに独立しています。
Size は図形自体の幅・高さ、Scale は子を含む座標系の拡縮です。Anchor の変更時も見た目を保ちます。
縦横の拡縮と回転の組み合わせで必要になる場合だけ、歪みを保つ Shear が現れます。
Group は従来の連結選択として別に使えます。親の表示・透明度や描画順は継承しません。
拡縮が 0 の親には付けられません。親の変更前後で、中間キーフレームと移動パスは親座標の値を保つため、途中の動きは調整が必要な場合があります。

角のドラッグは縦横比を保った拡大縮小、Shift を押しながらのドラッグは自由な比率の変更です。
選択候補・ロック・ドラッグ中の値・操作の完了や中止を画面で確認できます。

| 操作 | ショートカット |
| --- | --- |
| Undo / Redo | Ctrl/⌘ + Z / Ctrl/⌘ + Shift + Z |
| コピー / 貼り付け / 切り取り | Ctrl/⌘ + C / V / X |
| 元の位置に貼り付け | Ctrl/⌘ + Shift + V |
| 現在の Composition で非表示にする | Delete / Backspace |
| 1 px / 10 px 移動 | 矢印キー / Shift + 矢印キー |

キーボードアイコンからショートカット一覧を開けます。文字入力中や IME 変換中は編集用ショートカットと区別します。

## 画像・音声・動画を使う

画像は **Add image**、ドロップ、クリップボードから追加できます。PNG・JPEG・WebP の静止画像に対応します。
位置・サイズ・回転・不透明度・枠線・角丸・Glow と、他のオブジェクトと同じアニメーションを設定できます。

音声・動画は **Add media**、タイムラインの **音声を追加 / 動画を追加**、またはドロップから読み込みます。
MP4・WebM の動画と MP3・WAV・OGG・FLAC・M4A・WebM の音声が対象ですが、内包するコーデックの対応は端末に依存します。

- 動画はキャンバス上のオブジェクトになります。動画内の音声は独立した音声トラックへ分けます。
- 音声は Scene 時間を基準に配置します。動画と音声はそれぞれ開始位置やトリミングを変更できます。
- **Audio & video** のクリップをドラッグすると移動、両端をドラッグするとトリミングできます。選択後の数値入力も利用できます。
- 音声は音量・ミュート・削除に対応します。素材が最後の Composition より長い場合は Scene の再生時間も延びます。

取り込み・波形作成・転送には進捗と中止操作があります。素材は部屋へ保存され、共同制作者も同じ素材を使います。
保存ファイルには素材本体を埋め込むので、別の部屋へ読み込んでも元の素材 URL に依存しません。

## 共有チャットと AI

右側の **Chat** は部屋全体の会話です。通常のメッセージは人間同士の会話、`@codex` を付けた発言は AI への依頼になります。
AI を使うにはサーバーの API キー設定が必要です。

```text
@codex 選択した円を黄色にして
@codex 次の場面を追加して、円をベジェ曲線で右上へ動かして
@codex 数式を次の場面に追加して、Write で登場させて
@codex 円の X を途中で 300 にして、元の位置へ戻して
@codex 円を四角形の子にして、見た目は保って
@codex 透明背景の黄色い星を生成して右上に置いて
```

**Enter** で送ると提案を受け取り、依頼者が **Apply edits** で適用します。
**Ctrl/⌘ + Enter** は検証後に自動適用、**Shift + Enter** は改行です。
適用した編集は Undo で戻せます。提案・返答・適用状況・待機状態も共同制作者へ共有します。
実行の停止は依頼したブラウザから操作します。

選択中の対象が依頼の文脈になります。AI は現在の Scene 内を編集し、適用時に対象の変更・削除・ロックを再確認します。
画像は提案を返す前に生成・保存されます。Undo はオブジェクトの追加を取り消しますが、画像生成の API 利用や保存済み素材は取り消しません。
画素そのものの編集には対応していません。

不正な編集案は検証結果を添えて一度だけ修復します。API エラー・拒否・途中終了はこの修復の対象外です。
テキスト生成・修復・画像生成・画像保存は **全体で 170 秒**の期限を共有します。部屋の AI ロックは 180 秒です。
SDK の再試行も全体期限内に収めます。履歴は動画の Undo やプロジェクトファイルから分離しています。

## 保存・再接続・書き出し

編集内容はサーバーとブラウザ内へ保存し、再接続時に同期します。再生位置は各自で独立しています。
Undo は自分の編集を対象としますが、共同制作者の変更を守るため、新しく作ったオブジェクトを保持する場合があります。
他の人が親子付け後の座標や動きを編集した場合は、その親子関係と座標変換も保持して通知します。色などの独立した変更は取り消せます。
他の人が編集した新しい中間点や、子の親として使い始めたオブジェクトも、作成者の Undo では消しません。
オフラインの相手による未受信の編集まで検知できるわけではありません。

**Save project** は素材を含む `.poietra.json` を保存します。
親子関係・アンカー・拡縮・中間点を使うと保存形式は version 2 になります。旧 version 1 も読み込めます。
以前から開いたままの編集画面は再読み込みしてください。
**Open project** は新しい部屋に読み込み、同期の応答を待って開きます。現在の共同編集を上書きせず、チャット履歴も読み込みません。

**Export** は開始時の編集状態を固定し、処理中の共同編集に影響されず書き出します。

| 項目 | 選択肢 |
| --- | --- |
| 範囲 | Entire project / Current scene |
| 形式 | MP4（H.264）/ WebM（VP9、非対応時は VP8） |
| 解像度 | 原寸 / 720p / 1080p |
| フレームレート | 24 / 30 / 60 fps |

音声付き MP4 には端末の AAC エンコード対応が必要です。利用できない場合は WebM（Opus 音声）を選んでください。
**Cancel** で中断、完了後は **Download again** で再ダウンロードできます。

## 設定

以下のコマンドは **`apps/studio`** で実行します。

```sh
cp .env.example .env
```

ローカル Node は `.env` を読みます。キーなしでも編集・通常のチャット・書き出しを利用できます。
設定のサンプルは [.env.example](.env.example)、Worker の変数は [wrangler.jsonc](wrangler.jsonc) が基準です。

| 環境変数 | サンプル設定・既定値 | 用途 |
| --- | --- | --- |
| `OPENAI_API_KEY` | 未設定 | サーバー側の AI 認証 |
| `OPENAI_MODEL` | `gpt-6-astra` | 編集案の生成 |
| `OPENAI_IMAGE_MODEL` | `gpt-image-1` | 画像素材の生成 |
| `OPENAI_IMAGE_QUALITY` | `medium` | 画像品質 |
| `OPENAI_REASONING_EFFORT` | `medium` | 推論量。`default` でパラメータを送らない |
| `OPENAI_SERVICE_TIER` | `fast` | 優先度。`default` でパラメータを送らない |
| `PORT` | `5173` | Node の HTTP/WebSocket ポート |
| `POIETRA_DATA_DIR` | `.data` | Node の保存先。アプリの作業ディレクトリからの相対パス |
| `AUTH_ORIGIN` | `http://localhost:5173` | OAuth 復帰先の origin |

モデル・契約が推論量や優先度の指定を 400 で拒否した場合は、その指定を外して再送します。
ここに記載したモデル名はリポジトリの設定値であり、利用アカウントでの提供可否を検証したものではありません。

### 任意ログイン

アカウント周りは次の概念に分けています（2026-09-20 整理）。

| 概念 | 役割 |
| --- | --- |
| アカウント | 端末をまたいで自分の一覧を保持する本人の識別。 |
| ログイン方法 | Google / GitHub による本人確認。現在はそれぞれ別アカウントで、連携機能は未実装。 |
| セッション | このブラウザのログイン状態。有効期間は 7 日。ログアウトはこのセッションを失効させる。 |
| 共同編集の表示名 | 参加者・チャットに表示する名前。このブラウザに保存し、ログイン先の名前とは独立して変更できる。 |
| 自分のプロジェクト一覧 | 共有プロジェクトへの個人用ショートカット。本人だけが見られ、プロジェクトの所有権を表さない。 |
| 共有プロジェクト（ルーム） | リンクを知っている人が一緒に編集する場所。ゲストも参加できる。 |

ロゴの **Projects** でアカウントと自分の一覧を開き、左下のアバターで **共同編集の表示名と共有** を開きます。
ログイン中に開いたプロジェクトは一覧へ自動登録され、登録済みなら「一覧に追加済み」と表示します。
一覧から外した状態はアカウント側に保持するため、再読み込み・別端末・自動的なタイトル更新では復活しません。
「このプロジェクトを一覧に追加」で明示的に戻せます。ファイルへの保存は別の操作です。
一覧の「更新」はそのショートカットの更新日で、共有プロジェクトの最終編集日時ではありません。
ログイン状態の確認に失敗した場合は再試行を表示し、ゲストと判定しません。

Google / GitHub は片方だけでも設定できます。各プロバイダーに OAuth アプリを用意し、次の値をサーバーへ設定します。

| プロバイダー | 変数 | ローカル Node の callback |
| --- | --- | --- |
| Google | `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` | `http://localhost:5173/api/auth/callback/google` |
| GitHub | `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` | `http://localhost:5173/api/auth/callback/github` |

ブラウザの URL、`AUTH_ORIGIN`、登録した callback のホスト・ポートを一致させます。
ローカル Worker では `http://localhost:8787` に置き換えて `.dev.vars` に設定します。
配置する場合は、その環境専用の origin と秘密情報を用意してください。

セッションと本人用プロジェクト一覧は共同編集ドキュメントから分離しています。一覧から外しても部屋は削除しません。
Google と GitHub をメールアドレスで自動統合せず、共有リンクの編集権限もログインでは制限しません。
一覧から外した記録はルーム ID のみを保持し、タイトルは残しません。Node の既存ファイルと Worker の既存一覧を読み込み、
アカウント ID・セッション・部屋の保存形式・保存先は維持します。一覧に表示できる上限は 500 件で、外した記録は含めません。
認証・保存の 23 テスト、アカウントとプロジェクト操作の 12 ブラウザテスト、実 workerd の 3 回の再起動を伴う検証で、
削除・自動登録の競合、再追加、別アカウントの分離、ゲストの共同編集を確認しました。
認証テストはプロバイダー HTTP を模擬しています。本番では GitHub 認証の開始、フローの保存、
GitHub へのリダイレクトを確認しました。実プロバイダーでのログイン完了と有料 AI 呼び出しは今回の公開確認に含みません。

## 実行と配置

リポジトリルートの `pnpm build` を実行した後、次を利用できます。

```sh
# apps/studio
pnpm start             # Node で本番ビルドを配信
cp .dev.vars.example .dev.vars
pnpm dev:worker        # localhost:8787、ローカル workerd/SQLite/R2
pnpm run deploy        # ビルドと wrangler deploy --dry-run のみ
```

Node はローカルファイル、Worker は SQLite Durable Objects と非公開 R2 を使います。
編集データ・素材の参照・容量管理は部屋に、素材本体は R2 に置きます。アップロード完了後に参照を確定し、途中の素材を公開しません。
旧 SQLite 素材の読み出しと、ハッシュ・サイズ確認後の R2 への切り替えにも対応します。今回の公開では保存先を維持し、素材の一括移行は行っていません。

既定の設定はローカル検証用の `poietra-moonbit` です。本番は `--env production` を明示し、
Yumaboda アカウントの既存 `poietra-hackathon` Worker を更新します。3 種類の Durable Object、
`poietra-assets-prod` バケット、認証・AI の秘密情報、`poietra.com` と旧 workers.dev の接続先を維持します。

ブラウザ配信の共通コードをまとめた `0cda0c5` は **2026-09-20 17:32 JST** に公開しました。
Worker バージョンは `9d95a13c-c383-4c14-89f5-4af572ab85a4`（100%）です。
[CI 全項目](https://github.com/Poietra/poietra/actions/runs/35499218054)の成功後に切り替え、保存先・認証など16個の接続設定を維持しました。
本番で既存の検証ルーム・R2画像の復元、2ブラウザの編集と選択的 Undo、再生・シーク、実際の MP4 保存と102フレームの復号を確認しています。
レイアウト修正も維持しています。完全な外部ログインと有料 AI 呼び出しは今回の確認に含みません。
ロールバック先は直前の `83767c03-c8ae-49bb-8151-194d35af64a3` です。

文字の重なりを修正した `0981b5f` は **2026-09-20 15:34 JST** に公開しました。
当時の Worker version は `56c37647-0bc4-4a51-9089-7238ad8b6425`、配信率は 100% でした。
素材追加・解像度表示の余白と折り返し、Scale / Anchor のラベル配置を修正しました。
ビルド・関連ブラウザテスト 26 件と、本番の幅 760〜1920px の 7 段階・125% 拡大表示を確認しています。
本番で既存ルーム・素材の復元、共同編集・Undo・102 フレームの MP4 出力も確認しました。
保存先と実行設定を維持し、復帰先は `47fbb2cc-fc00-489f-8f4d-cf6e62b95337` です。

UI の購読を分離した `fa0b627` は **2026-09-20 15:18 JST** に公開しました。
当時の Worker version は `47fbb2cc-fc00-489f-8f4d-cf6e62b95337`、配信率は 100% でした。
[実装版の CI](https://github.com/Poietra/poietra/actions/runs/35493085372) は全項目を通過しています。
16 個の binding と実行設定、保存モデルを維持し、本番の JS/WASM、既存ルームと R2 画像、
共同編集・Undo、SVG 要素の保持、ゲスト画面、GitHub 認証開始と共同編集名を確認しました。
分離した時計で再生・一時停止・シークと表示中の Composition への復帰を確認し、
本番から出力した MP4 の全 102 フレームを復号しています。検証には専用ルームを使い、
実プロバイダーでのログイン完了・有料 AI 呼び出しは含めていません。
記録した復帰先は `b5709485-5574-4ff9-8bf5-f242e76bb355` です。

描画改善版 `8357a35` は **2026-09-20 13:54 JST** に公開しました。
当時の Worker version は `b5709485-5574-4ff9-8bf5-f242e76bb355`、配信率は 100% でした。
[実装版の CI](https://github.com/Poietra/poietra/actions/runs/35489644696) は全項目を通過しています。
保存先・実行設定・保存形式を維持し、本番の JS/WASM、既存ルームと R2 画像、共同編集・Undo、ゲスト画面、
GitHub 認証開始と共同編集名を確認しました。移動・色変更時に SVG の要素を保持し、
本番から出力した MP4 の全 102 フレームを復号できることも確認しています。

アカウント整理を含む `54d0314` は **2026-09-20 13:10 JST** に公開しました。
当時の Worker version は `8b1850ca-7509-4c64-82d3-6ddebdf00743`、配信率は 100% でした。
[実装版の CI](https://github.com/Poietra/poietra/actions/runs/35487784040) は全項目を通過しています。
本番で JS/WASM の一致、既存 version 2 ルームと R2 画像の復元、画像の重複排除、2 ブラウザの共同編集・Undo、
GitHub 認証開始、ゲストのアカウント画面、共同編集名の同期と再読み込み後の保持を確認しました。
実プロバイダーでのログイン完了は検証に含みません。以前から開いている画面は再読み込みすると明示的な再追加を使えます。

親子関係・中間点を含む `4392255` は **2026-09-20 05:34 JST** に公開しました。
当時の Worker version は `7cc7168f-e149-4170-9078-b245c3cde006`、配信率は 100% でした。
保存先・設定・JS/WASM の一致、既存ルームと画像の復元、2 ブラウザの共同編集と Undo に加え、
本番での Scene 再生と MP4 の保存・全フレームの復号を確認しました。検証には専用の確認用ルームを使っています。
親子付け・中間点の同期と保存、変換後のドラッグ・リサイズ、version 2 の保存・新しい部屋への読み込みも本番で確認しました。
[公開版の CI](https://github.com/Poietra/poietra/actions/runs/35467081415) は全項目を通過しています。以前から開いている編集画面は再読み込みしてください。
性能改善版 `0aa7fa1f-ac38-46b8-bc8a-8ea4b1fea4b9` は version 2 を扱えず、親子関係版も一覧から外した記録には未対応です。
復帰には両方の保存データに対応したコードを使います。Worker の版を戻しても保存データは戻りません。

初回公開はソース `7b8c903`、Worker version `b9df5edd-9b0c-44f3-a488-1db05f9b8049` です。
公開前後で保存先 ID と設定を照合し、公開された JS/WASM とビルド成果物のハッシュの一致を確認しました。
専用の確認用ルームで、旧版が保存したデータ・画像の復元、画像の重複排除、2 ブラウザでの共同編集、
相手の色変更を保つ Undo、再読み込みと新しい WebSocket 接続後の復元を確認しました。

```sh
# apps/studio
pnpm build:web
pnpm check:production
pnpm exec wrangler whoami
pnpm exec wrangler deployments list --env production  # 復帰先の version ID を記録
pnpm upload:production                              # ビルドとアップロード、配信はまだ切り替わらない
pnpm exec wrangler versions view VERSION_ID --env production --json
pnpm deploy:production VERSION_ID@100 --yes           # アップロードされた ID を指定
```

切り替え後は公開 URL で動作を確認します。復帰には、記録した以前の version ID を同じコマンドで 100% に戻します。
コード・配信アセットの復帰であり、編集データの巻き戻しではありません。
初回公開の復帰先と詳細は[ルート README](../../README.md#deployment-and-limits)に記載しています。

トップページは英日 HTML と Markdown をビルド時に生成します。言語は明示 URL、保存済み設定、ブラウザ設定の順で扱い、未対応言語は英語へ戻します。
サーバーの初期 HTML は URL と `Accept-Language`、操作接続後はブラウザ保存済み設定も使います。`/ja/` で日本語を明示できます。
編集画面全体の翻訳は含みません。

公開ページは `Accept: text/markdown` に対応し、`robots.txt`・`sitemap.xml`・`llms.txt` も生成します。
編集画面と共有ルームは `noindex` ですが、これはアクセス制御ではありません。
文言・画像を変更したときの素材再生成は `node scripts/build-home-assets.mjs` で行います。
その場合だけ FontTools/Brotli の `pyftsubset` と libwebp 対応 FFmpeg が追加で必要です。

## 検証と性能

[ルートの Checks](../../README.md#checks) に確認済み範囲と実行コマンド、
[Performance](../../README.md#performance) に 2026-09-19〜20 の再計測結果・条件・全試行データをまとめています。
数値評価、Yjs snapshot、AI 提案コンパイル、実際の 2/4 人編集、トップページ、描画、配信サイズを区別して測っています。

`0cda0c5` では、ブラウザ用に別々にリンクしていた MoonBit コードを共通化しました。
初期 JS は raw **1,883,417 → 1,610,705 B（−14.5%）**、gzip **524,141 → 457,443 B（−12.7%）**です。
同一端末・低速回線を模したコールド起動3回ずつで、ステージ準備の中央値は **11.90 → 10.65秒（−10.5%）**になりました。
ローカルの非圧縮配信・図形1個での結果であり、本番 CDN や実端末の待ち時間を保証する値ではありません。
別の500図形計測では、入力受信→DOM が中央値14.4 ms、描画待ち2回を含む反映が33.4 msでした。
再生間隔 p95 は49.9 ms、最大83.4 msで、長いフレームは残ります。
[測定条件・全試行・CPU記録](../../README.md#shared-browser-linking--2026-09-20)を保存しています。

`99a349d` では文書編集中の表示内容と素材準備を再利用しました。位置変更12回の開発用計測で、
素材の再準備は **17 → 0 回**、Scene タブ・レイヤー・タイムライン構造・素材一覧の本体実行は各 **46 → 0 回**でした。
ルートと Context の接続部分は変更後も24回実行されます。StrictMode の回数で、本番の時間計測とは別です。
本番ビルドの追加診断では、500図形の入力受信から DOM 反映までが中央値 **20.8 → 13.7 ms**でした。
ただし描画待ち2回を含むドラッグ中央値は **33.4 → 42.0 ms**と増え、入力の実際の到着間隔も変わっています。
再生 p95 も試行により異なり、長いフレームは残ります。体感速度が一様に改善したとは判断していません。
初期 JS gzip は 524,141 B（+0.29%）です。[全試行・条件・CPU記録](../../README.md#document-edits-and-resource-preparation--2026-09-20)を保存しています。

`fa0b627` では UI の購読を分離しました。開発用 StrictMode で、再生 24 フレーム中の
レイヤー一覧・タイムライン本体・チャットなどの実行が各 **48 → 0 回**、在室者更新 12 回による
文書パネルの実行が各 **24 → 0 回**になりました。時間表示と共同編集への追従は維持しています。
500 オブジェクトの再生間隔 p95 は **50 → 33.5 / 49.9 ms**（最終版を 2 回計測）でしたが、
長いフレームは残り、ドラッグ中央値はほぼ同じです。同期型の時計で 66.7 ms へ悪化した途中版は採用していません。
初期 JS gzip は 522,608 B（+0.27%）で、起動・書き出しの高速化を示す変更ではありません。
[条件・全試行・途中版との比較](../../README.md#ui-subscription-boundaries--2026-09-20)を記録しています。

`8357a35` では、共通の型付き SVG モデルから画面の要素を差分更新し、不透明でエフェクトのない図形を
Canvas に直接描くようにしました。半透明と Glow は合成単位を維持します。
`4b2ae74` を別 checkout で再ビルドした比較では、500 図形の MP4 書き出しが **388 → 342 ms（12%短縮）**、
その描画部分が **155 → 125 ms（20%短縮）**でした。100 図形のドラッグ中央値は **38.5 → 33.5 ms**ですが、
500 図形では **34.4 → 33.4 ms**と小幅です。再生の p95 は 50 ms のままで、長いフレームは残っています。
起動は改善していません。初期 JS gzip は 519,548 → 521,212 B、制限付きの非圧縮ローカル配信での
キャンバス準備は 11.67 → 11.84 秒でした。実機 fps・本番ネットワークの値ではありません。
[計測条件・全試行・比較結果](../../README.md#retained-svg-and-direct-shape-painting--2026-09-20)に記録しています。

設計上、型付き編集計画・不変 snapshot・プレビューと書き出しの共通評価器は分離されています。
2026-09-20 に UI の購読も分離しました。ローカルの再生位置は `StudioPosition` が保持し、
キャンバス・音声・時間表示だけが購読します。編集データと選択の Context に時刻通知を流さず、
在室者・接続・Undo 状態は必要な表示で購読し、チャットの既読位置はパネル内に保持します。
時計専用の Provider は React の通常の状態更新で同じ時刻を配信し、共有文書は同期的な外部ストア購読を維持します。
時間から現在の区間を求める処理は派生値とし、保存モデルと共同編集の仕様は維持します。
新しい UI は必要な購読元を選び、互換用の集約 `useEditor` を通常の属性パネルに使わない方針です。
文書編集についても、Scene タブ・レイヤー一覧・タイムライン構造・素材トラックの表示入力を分けました。
位置だけの編集では内容を作り直さず、名前・選択・表示状態・順序・時間など、表示に必要な変更には追従します。
描画の準備は各 Composition の文字・数式と表示対象の画像素材を比較し、位置・色・時間だけの変更では再実行しません。
準備に失敗した場合は、編集データを変えずに画面から再試行できます。Canvas の古い描画結果の抑制も維持します。
ルートと Context の接続部分は文書編集を引き続き受け取ります。残る整理対象はこれらの購読、
他のパネル、初期読み込み、一部の `Any` による処理です。

親子関係・中間点を追加した `4392255` を再計測しました。500 オブジェクト・合計 3,000 中間点の CPU 評価は
**0.549 ms/フレーム**、親の編集・Yjs/Undo 記録・snapshot・Composition 評価は **1.222 ms**でした。
子の snapshot を作り直さないことも確認しています。描画・ネットワークの時間は含みません。
既存デモ（3.4 秒、720p/30fps）のエンジン書き出しは MP4 **236 ms**、WebM **270 ms**で、全 102 フレームを確認しました。
初期 JS は新機能分だけ gzip で **26,065 B 増加**しています。低速回線・CPU 制限下の非圧縮ローカル配信では
キャンバス準備が中央値 **11.58 秒**となり、前回の記録より約 0.3 秒増えました。本番の体感や実機 fps とは区別します。
[新機能追加後の計測](../../README.md#parent-transforms-and-value-curves--2026-09-20)に条件、ばらつき、全試行を記載しています。

2026-09-20 の起動・再生・ドラッグの重さの報告を受け、編集時に読む範囲を現在の Composition に絞り、
変更のないレイヤー行と静止区間の描画を再利用しました。書き出しでも静止区間を再描画せず、全フレームのエンコードは維持します。
同じ端末・Chromium・ソフトウェア GPU の計測では、500 オブジェクトのドラッグ反映の中央値が **61.7 → 50.0 ms**、
再生中の rAF 間隔の中央値が **33.3 → 16.7 ms**、500 図形の 720p/30fps MP4 書き出しが **870 → 444 ms** でした。
100 オブジェクトのドラッグ中央値は変わらず、再生にも長い更新間隔が残ります。実機の fps や INP の値ではありません。
書き出し・ファイル操作の遅延読み込みと検証スキーマの必要時生成により、編集開始時の JS は gzip 換算で **563,197 → 492,567 B（12.5% 減）**です。
低速回線・CPU 制限を設定したローカルの非圧縮配信では、キャンバス準備が **12.78 → 11.28 秒**でした。本番の読み込み時間とは区別してください。
[高速化した際の計測](../../README.md#startup-dragging-playback-and-webcodecs--2026-09-20)に条件、ばらつき、全試行と再現手順を記載しています。

2026-09-19 に、動画の連続デコードと Canvas への直接描画を導入しました。
実際のフレーム時刻で再利用を判断し、通常の Canvas 描画では PNG への変換を省きます。
シーク・中断・同じ素材の異なる時刻の同時表示を検証し、SVG への代替経路も維持しています。
動画・音声のライブラリは素材操作や書き出し時に読み込むよう変更しました。
720p 動画の連続描画は、同条件のソフトウェア GPU 計測で p50 が **53.0 → 12.8 ms** になりました。
編集開始時の JavaScript は gzip で **685,047 → 560,586 B（18.2% 減）**です。
この 2026-09-19 の変更では、大量の図形を共同編集する応答時間は改善しておらず、悪化した値も含めてルート README に記録しています。
CPU の高速化率を UI 全体の速度や再生 fps として説明しないでください。
描画計測は SwiftShader を使う環境の値であり、実機 GPU や本番ネットワークでの値ではありません。

2026-09-20 に、手動追加・ドラッグ・非表示・グループアニメーションの判断を純粋な MoonBit の型付き計画へ移しました。
追加・貼り付け・素材取り込みでは `ObjectInsertion` を共用し、変更のない座標を同期しない既存の処理と Undo を維持しています。
500 個を全選択する部品計測では、連結対象の検索が **0.605 → 0.111 ms（5.5 倍）**、ドラッグ計画が **0.628 → 0.108 ms（5.8 倍）**になりました。
JS との変換を含みますが、Yjs への反映・描画・通信は含みません。3 プロセスの生データ・ばらつき・再現手順は
[型付き編集操作の計測](../../README.md#typed-editing-commands--2026-09-20)を参照してください。

同日、Transition の長さ変更と属性別 Timing の更新も、純粋な MoonBit の型付き計画へ移しました。
時間範囲だけを読み、変わった開始時刻・長さだけを同期します。Transition の短縮が、オフラインの共同制作者による
同じ属性のイージング・開始時刻の編集を消す不具合を再現し、同期後と Undo/Redo 後にも保持するよう修正しました。
500 トラックの部品計測では、延長が **0.321 → 0.099 ms（3.3 倍）**、共通時間の短縮が **0.321 → 0.161 ms（2.0 倍）**、
全 11 属性の短縮が **1.258 → 1.044 ms（1.2 倍）**になりました。Yjs への反映・描画・通信は含みません。
[時間編集の計測](../../README.md#transition-timing--2026-09-20)に全条件と生データを掲載しています。

単体トラックの `setTrack` も、継承・検証・変更計画を型付きコアへ移しました。
この API 経由で属性別 Timing を編集しても親マップを保ち、共同制作者の曲線を同期後と Undo/Redo 後にも保持します。
ウォームアップ後の部品計測では、長さ変更が **0.848 → 0.287 µs（3.0 倍）**、継承の確定が **1.378 → 0.423 µs（3.3 倍）**です。
Yjs への反映・描画・通信を含まない測定条件と生データは[単体トラックの計測](../../README.md#single-track-timing--2026-09-20)を参照してください。

Scene／Composition の追加も型付きコアへ移し、初期値の生成をサンプル・複製・削除時の遷移生成と共用しました。
Composition の複製にも 100 件の上限を適用し、共有先の確認と変換をすべて済ませてから追加を反映します。
途中で変換に失敗した場合に、データや Undo 履歴、削除済みの元状態の復活だけが残らないことを検証しています。
実際の Yjs への書き込みと Undo 記録を含む追加処理は、500 オブジェクトで **4.04 → 4.09 ms**でした。
測定のばらつきもあるため、この変更の速度改善は確認できていません。
[作成処理の計測](../../README.md#composition-creation--2026-09-20)に条件と全試行を掲載しています。

## 機能を追加する

ドキュメントの型は `moonbit/scene`、編集操作や画面は各 MoonBit パッケージに追加します。
`pnpm build:moonbit` で変換コード・公開レコード型・単純な JS の公開窓口を再生成します。
型を追加するだけで UI や保存時の検証が自動実装されるわけではありません。
オブジェクトの追加方式を増やす場合は `editor.ObjectInsertion` で配置と初期トラックを計画し、変換後に一括反映します。
属性別アニメーションは `scene/editing` の属性一覧・取得・置換を共用し、`editor` の型付き計画で変更対象を決めます。
Scene／Composition の初期値は `scene/creation`、作成の判断と上限チェックは `editor/structure_commands` を更新します。
Yjs は途中の書き込みをロールバックしないため、ホスト境界で変換と共有先の検証を済ませてから反映します。
既存の Timing を更新するときは、共同編集の親マップを保ち、変更したフィールドだけを書き込みます。
編集コアは JS と WASM の両方でテストし、ホスト境界は既存の公開 API・共同編集・ブラウザテストで確認します。
[拡張手順](../../README.md#add-a-feature)に、各層の役割と必要な確認をまとめています。

`pnpm test:extensions` は隔離したコピーへ入れ子の型と API を追加し、生成・コンパイル・JS からの呼び出し・型の誤用検出まで実行します。
`pnpm typecheck` は既存 108 モジュールの公開契約との互換性も確認します。
これらはリポジトリルートから実行してください。

## 現在の制限

| 対象 | 制限 |
| --- | --- |
| プロジェクトファイル | 128 MiB。1 Scene あたり 500 オブジェクト、100 Compositions |
| 画像の取り込み | 一度に 8 枚、元画像 20 MiB・4,000 万画素まで |
| 保存する画像 | 長辺 2,048 px・1 MiB 以下へ正規化、部屋合計 64 MiB |
| 音声・動画 | 32 MiB/ファイル、元素材 10 分以内、部屋合計 128 MiB |
| 中間キーフレーム | 1 オブジェクトの Transition に削除印を含め 1,024 点まで |
| AI | 現在の Scene 内。1 提案で末尾への Composition 追加 4 個、画像生成 2 枚まで |

保存済み素材は Undo などの参照を保持するため自動削除しません。
デスクトップ Chromium を主な検証対象とし、長時間・大量素材・WAN 上の多数参加者での性能は未計測です。
画像の画素編集、任意シェーダーの持ち込み、課金・テナント管理・複雑な権限管理は対象に含みません。
Glow は WebGL2 を使い、利用できない場合は SVG / Canvas2D に戻ります。
